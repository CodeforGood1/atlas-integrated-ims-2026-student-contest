# Atlas Integrated IMS

Industrial monitoring software with a web interface, backend services, machine-learning inference, and gateway connectivity.

## Run locally

```bash
pip install -r requirements.txt
python backend/app.py
```

In another terminal:

```bash
cd frontend
npm install
npm run dev
```

Configure local environment values as needed before deployment.
